# AUTO-GENERATED BY setup.sh — DO NOT EDIT.
# Edit setup.conf instead. Regenerate via ./build.sh --setup or ./run.sh --setup.
services:
  devel:
    build:
      context: .
      dockerfile: Dockerfile
      target: devel
      args:
        APT_MIRROR_UBUNTU: ${APT_MIRROR_UBUNTU:-archive.ubuntu.com}
        APT_MIRROR_DEBIAN: ${APT_MIRROR_DEBIAN:-deb.debian.org}
        TZ: ${TZ:-Asia/Taipei}
        USER_NAME: ${USER_NAME}
        USER_GROUP: ${USER_GROUP}
        USER_UID: ${USER_UID}
        USER_GID: ${USER_GID}
    image: ${DOCKER_HUB_USER:-local}/urg_node_noetic:devel
    container_name: urg_node_noetic${INSTANCE_SUFFIX:-}
    privileged: ${PRIVILEGED}
    ipc: ${IPC_MODE}
    stdin_open: true
    tty: true
    cap_add:
      - SYS_ADMIN
      - NET_ADMIN
      - MKNOD
    security_opt:
      - seccomp:unconfined
    network_mode: ${NETWORK_MODE}
    environment:
      - DISPLAY=${DISPLAY:-}
      - WAYLAND_DISPLAY=${WAYLAND_DISPLAY:-}
      - XDG_RUNTIME_DIR=${XDG_RUNTIME_DIR:-/run/user/1000}
      - XAUTHORITY=${XAUTHORITY:-}
    volumes:
      - /tmp/.X11-unix:/tmp/.X11-unix:ro
      - ${XDG_RUNTIME_DIR:-/run/user/1000}:${XDG_RUNTIME_DIR:-/run/user/1000}:rw
      - ${XAUTHORITY:-/dev/null}:${XAUTHORITY:-/dev/null}:ro
      - ${WS_PATH}:/home/${USER_NAME}/work
    devices:
      - /dev:/dev
    deploy:
      resources:
        reservations:
          devices:
            - driver: nvidia
              count: all
              capabilities: [gpu, compute, utility, graphics]

  runtime:
    extends:
      service: devel
    build:
      context: .
      dockerfile: Dockerfile
      target: runtime
    image: ${DOCKER_HUB_USER:-local}/urg_node_noetic:runtime
    container_name: urg_node_noetic-runtime${INSTANCE_SUFFIX:-}
    stdin_open: false
    tty: false
    profiles:
      - runtime

  test:
    build:
      context: .
      dockerfile: Dockerfile
      target: test
      args:
        APT_MIRROR_UBUNTU: ${APT_MIRROR_UBUNTU:-archive.ubuntu.com}
        APT_MIRROR_DEBIAN: ${APT_MIRROR_DEBIAN:-deb.debian.org}
        TZ: ${TZ:-Asia/Taipei}
        USER_NAME: ${USER_NAME}
        USER_GROUP: ${USER_GROUP}
        USER_UID: ${USER_UID}
        USER_GID: ${USER_GID}
    image: ${DOCKER_HUB_USER:-local}/urg_node_noetic:test
    profiles:
      - test
