name: Release

on:
  workflow_call:

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Create release archive
        run: |
          VERSION="${GITHUB_REF_NAME}"
          ARCHIVE_NAME="urg_node_noetic-${VERSION}"

          mkdir -p "${ARCHIVE_NAME}"
          cp -r Dockerfile compose.yaml \
                build.sh run.sh exec.sh stop.sh \
                .env.example .hadolint.yaml script/ test/smoke_test/ docker_setup_helper/src/ \
                README.md doc/ "${ARCHIVE_NAME}/"

          tar czf "${ARCHIVE_NAME}.tar.gz" "${ARCHIVE_NAME}"
          zip -r  "${ARCHIVE_NAME}.zip"    "${ARCHIVE_NAME}"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          prerelease: ${{ contains(github.ref_name, '-') }}
          files: |
            urg_node_noetic-*.tar.gz
            urg_node_noetic-*.zip
